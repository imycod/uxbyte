一个含代码自动生成、自动写入文件、自动打包、自动纠错的项目。

注意事项：
1. 使用前请更换 ux-server/config.js llm host 地址
2. 进入ux-server 执行 npm run start & 进入 ux-client 执行 npm run preview
然后访问 http://localhost:3000/index.html

windows：
模型生成的文件会在 d:/frontend-code/{clientId}/{projectId} 备份一份。